# 진행 상황

> 이 파일은 대화(세션)가 바뀌어도 다음 작업을 이어갈 수 있도록 현재까지의 진행 상황, 결정 사항, 남은 일을 기록합니다.
> 기획·요구사항의 기준 문서는 항상 [PACK_OPENING_SPEC.md](PACK_OPENING_SPEC.md)이며, 이 파일은 그 진행 로그입니다.

최종 갱신: 2026-09-25 · M4 구현 완료 (등급별 카드 재질 + 테두리 암시 발광 + 등급별 등장 연출) — 등장 연출은 사용자 브라우저 검증 대기

## 지금 상태 한 줄 요약

M3까지 커밋·배포 완료(GitHub Pages). M4 중 등급별 카드 재질 + 뒷면 테두리 암시 발광은 커밋(`23b9284`), 뒤집을 때 등급별 등장 연출(반짝임 + 최고등급 플래시·빛 고리·입자)은 구현 완료·사용자 검증 대기. 다음은 M5(요약 화면 + 선택·클로즈업 + 결과 화면). 실제 경품/확률 데이터는 사장님 확정 대기 → 그때까지 팩 결과는 초안 5장 고정.

## 완료된 작업

### M0-1. 로고 SVG 재작성

- `logo.svg` (금색 그라디언트), `logo-mono.svg` (currentColor, 단색) — 자동 트레이싱이 아니라 원본 `logo.png`(410×366 목업)의 중심선을 좌표 단위로 다시 그린 벡터.
- 구조: `#wordmark`(워드마크, stroke 3.6) / `#emblem`(해·조개·물고기, stroke 4.3), viewBox `52 34 306 267`(원본 PNG 좌표계 그대로 사용).
- 워드마크는 폰트가 아니라 원본 글자 형태를 따라 그린 패스(명세 4.1 요구사항).
- 물고기 두 마리가 겹치는 4곳 중 2곳은 원본처럼 한 선이 다른 선 아래로 지나가며 살짝 끊김(weave) — `fish-b-2` 세그먼트를 마스크로 투명 처리해 구현.
- 검수 이미지: `design/logo-overlay-check.png` (원본 PNG 위에 SVG를 빨간선으로 오버레이).
- **확정되지 않은 부분:** 원본이 작아서(410px) 해석이 들어간 값들 — 교차부 끊김 폭, 물고기 눈 크기, 선 굵기. 사장님이 "원래는 이렇다"고 지적하면 수치 조정으로 반영 가능.

### M0-2. 팩·카드 2D 시안

`design/` 폴더에 브라우저로 바로 보는 시안 페이지로 구현(별도 이미지 파일이 아니라 SVG를 생성하는 JS 함수들 — M1에서 Three.js 텍스처로 그대로 재사용할 계획).

- `design/index.html` — 전체 갤러리 페이지 (로고 3종, 팩 앞/뒤, 카드 뒷면 레어도별 4종, 카드 앞면 5종, 팔레트·서체, 확인 필요 사항 목록).
- `design/render.html` — 에셋 1개를 원본 크기로 렌더 (쿼리스트링: `?a=pack-front|pack-back|card-back|card&id=<prize-id>`). 확대 검수·텍스처 추출용.
- `design/lib/common.js` — 팔레트(INK), 금속 그라디언트(METALS: gold/silver/ivory/rainbow), 종이결 필터, 박 찍힘(foil stamping) 필터, 방사선/물결/모서리 장식 등 공통 SVG 생성기.
- `design/lib/logo.js` — `logo-mono.svg`를 fetch해 원하는 위치·크기·색으로 인라인하는 헬퍼.
- `design/lib/icons.js` — 경품 카테고리 아이콘 4종(할인권/카드씰/카페음료/부스터팩).
- `design/lib/data.js` — 1차 목업 데이터: 등급 4종(RARITIES), 경품 5종(PRIZES). **명세 R5-1 데이터 모델과 필드 동일** → 2차에서 서버 API 응답으로 그대로 교체 가능하게 설계.
- `design/lib/pack.js` — 팩 앞/뒤 SVG. 레이어를 재질별로 분리(`[data-layer=paper|crimp|foil|print|shading]`)해 M1에서 텍스처/마스크 분리 작업이 쉽도록 함.
- `design/lib/card.js` — 카드 앞/뒤 SVG. 630×880(63:88mm 비율), 등급별 프레임 재질·일러스트 창 배경·등급 마크(진주 1~3알 + 최고등급은 해 문양) 자동 생성.

**로컬 확인 방법:** 프로젝트 루트에서 `python3 -m http.server` 실행 후 `http://localhost:8000/design/` 접속.

디자인 방향: 로고의 해·조개·물고기 모티프를 살려 "바다 위 해돋이"(포항/호미곶) 콘셉트로 통일. 재질 4단계(아이보리→은→금→무지개 포일)로 레어도를 구분.

### M0-2 후속. 팩 앞면 디자인 정리

사장님 1차 피드백 반영해 `design/lib/pack.js`의 `packFront` 정리:

- 포장지 안에 그려져 있던 카드 모양 사각 테두리(외곽 프레임 + 오목모서리 안쪽 사각형 + 모서리 부채꼴 장식)를 모두 제거.
- 해돋이 방사선 무늬 제거. 로고 위치 기준으로 계산하던 방사선 중심점(`logoPoint`/`LOGO_SUN`)도 같이 정리.
- 물결이 위 카드 사각형의 오목모서리 clip에 맞춰 잘려서 하단이 부자연스럽게 굴곡져 있던 문제 발견 → clip을 단순 사각형으로 교체.
- 물결 좌우 여백을 없애 팩 좌·우 끝에서 끝까지 이어지도록 수정(`inner.x0/x1`을 `0~W` 전체 폭으로 변경).
- "5 CARDS" 원형 메달 장식 제거(아래 안내 문구 "카드 5장을 모두 확인하고…"에 이미 정보가 있어 중복이었음).
- 텍스트·로고·물결 자체(모양)는 그대로 유지.

## 사장님 컨펌 결과 (2026-09-25)

1. **등급 체계** — 현재 시안(가칭 4등급 C/R/SR/UR 및 표기) 그대로 승인.
2. **문구** — 현재 자리 표시 문구(팩 제목·이벤트명·매장 정보 형식) 그대로 승인.
3. **경품 일러스트** — 현재 플레이스홀더 방식 그대로 승인.
4. **색감 톤** — 현재 팔레트(금색·베이지 톤) 그대로 승인.
5. **경품/확률 데이터** — 아직 미정. 사장님이 추후 직접 확정해서 전달 예정(`design/lib/data.js`의 `PRIZES`/`RARITIES` 확률 값만 교체하면 반영됨). M1 3D 작업은 이 값과 무관하게 목업 데이터로 먼저 진행 가능.

→ 위 1~4번 확정으로 M0 디자인 단계는 사실상 마무리. M1(Three.js 3D 구현)으로 넘어갈 준비 완료.

## M1. Three.js 셋업 + 3D 팩 + 포일 재질 + 틸트 (2026-09-25)

실행: `npm install` → `npm run dev` → `http://localhost:5173/` (README "실행 및 빌드" 참고).

구조 (`src/`):

- `main.ts` — 렌더러·카메라·루프, 인트로 → 팩 등장 흐름. 톤매핑은 `NeutralToneMapping`(시안 인쇄색 유지). 카메라 거리는 팩이 화면 높이 70% / 폭 74%를 넘지 않게 자동 계산.
- `core/spring.ts` — 스프링 솔버(고정 1/240s 서브스텝, 주파수·감쇠비로 설정). 명세 R7에 따라 모든 움직임은 이걸로.
- `core/tilt.ts` — 틸트 입력: 마우스 호버 / 드래그(떼면 복귀). (M1 때 자이로도 넣었으나 2026-09-25 사용자 결정으로 제거 — 폰 기울기 기능 불필요)
- `gfx/fonts.ts` — SVG를 이미지로 그릴 때 웹폰트를 못 쓰는 문제 해결: 실제로 쓰인 글자가 들어 있는 폰트 조각만 받아 data URL `@font-face`로 SVG 안에 삽입 (Google Fonts `text=` 서브셋 + Pretendard unicode-range 조각).
- `gfx/svg.ts` — SVG 문자열 → 캔버스.
- `gfx/surface.ts` — 높이맵 → 노멀맵, 블러, 값 노이즈.
- `gfx/env.ts` — 절차적 스튜디오 환경맵(PMREM). HDRI 파일 없이 소프트박스·대각선 스트립·광점을 배치해 기울일 때 금박 위로 하이라이트가 지나가게 연출.
- `gfx/backdrop.ts` — 배경(방사형 그라디언트 + 디더) + 팩 그림자.
- `pack/textures.ts` — `design/lib/pack.js`(M0 확정 시안)를 재질별로 래스터화: albedo(2D 전용 음영 레이어 제외, 박은 평평한 금색) / ORM(G=거칠기, B=금속도, 박 마스크 기반) / 노멀(박 눌림 + 크림프 골 + 뒷면 봉합선 + 종이결 + 봉합부 근처 주름). 텍스처 배율 1.5 (900×1560).
- `pack/pack.ts` — 앞·뒤 곡면이 좌우 접힌 가장자리와 상·하단 봉합부에서 만나는 "베개" 형태 메시. `MeshPhysicalMaterial`(클리어코트 = 포장 필름 광택, 약한 iridescence = 펄). 톱니 외곽은 알파 + alphaToCoverage. `packSurface(side, v0, v1)`로 v 구간만 만들 수 있게 해 둠 → M2에서 절취선 위/아래 분리용.
- `data/draw.ts` — `drawPack()`: 팩 결과(카드 5장). **확률 미정이라 추첨 없이 초안(`design/lib/data.js`)의 경품 5종을 등급 오름차순으로 그대로 반환** (사용자 지시, 2026-09-25). 2차에서 같은 시그니처로 서버 API 호출로 교체. 결과는 연출 시작 전(로딩 중)에 확정되어 `session.result`에 보관 → M3에서 사용.

연출:

- 인트로("화면을 터치해서 시작") → 팩이 아래에서 회전하며 스프링으로 등장 → 둥실 떠 있는 상태로 기울여 보기(최대 ±18°).
- 로딩 중에 탭해도 기억했다가 준비되면 바로 시작.
- 1.5초 이상 입력이 없으면 천천히 자동으로 흔들려 반사가 보이게 함.

검수 결과 (Chrome, M1 Mac):

- 정면·좌우·상하 기울기, 모바일 세로 비율(390×780), 뒷면(좌우 반전 없이 읽힘), 측면(접힌 가장자리) 캡처로 확인.
- 텍스처 생성 약 1.5초, 렌더 약 5ms/프레임(2880×1514).
- `npm run build` 통과. (당시 쓰던 개발용 검수 훅 `window.__dev`는 M2 때 삭제 — 브라우저 검증은 사용자가 직접 함)

**실기기 미검증 (M10 QA 전에 한 번은 확인 필요):**

- Safari/iOS에서 SVG 이미지 안 내장 폰트 적용 (타이밍 문제 대비로 decode 후 50ms 대기 넣어 둠).
- 중급 안드로이드에서 텍스처 생성 시간 (메인 스레드에서 수행 — 길면 Web Worker로 이전 검토).

알려 둘 점:

- 폰트는 런타임에 Google Fonts / jsDelivr에서 받음(시안 페이지와 같은 출처). 실패하면 시스템 폰트로 대체. 배포(M10) 때 셀프 호스팅 검토.
- 금박 색·거칠기, 환경 조명 배치는 화면 캡처 보며 1차 튜닝한 값. 사장님 피드백에 따라 `pack/textures.ts`의 `FOIL_ALBEDO`/`ROUGH`, `gfx/env.ts`의 패널 배치로 조정.

## M2. 절취 인터랙션 + 팩 뒤집기 (2026-09-25, 커밋 `80969d9`)

추가된 파일:

- `pack/tear.ts` — `TearState`: 절취선을 256칸으로 나눠 칸별 잘린 시각을 기록. 손가락이 허용 밴드 안에서 지나간 칸만 잘림 → 1:1, 양방향, 중간 시작, 떼었다 이어 자르기 모두 같은 방식. 셰이더에 1×256 텍스처로 전달(R = 벌어짐, G = 잔광). `finish()`로 95% 이후 남은 칸을 헤드 위치에서 양쪽으로 마저 찢음.
- `pack/tearShader.ts` — 팩 재질에 `onBeforeCompile`로 끼워 넣는 절취 표현. 윗조각/본체가 같은 불규칙 찢김 곡선(노이즈 3옥타브)의 반대쪽만 그림 → 안 잘린 곳은 이음새 없음. 잘린 곳은 틈 + 흰 종이 단면 + 잔광(HDR). 윗조각은 잘린 만큼 바깥으로 들뜸. 자르기 전에는 앞면 절취선을 따라 안내 반짝임이 흐름.
- `pack/cutter.ts` — 절취 입력. 화면 좌표를 절취선 높이 앞면 평면(`TEAR_Z`)과 교차시켜 팩 좌표(u, v)로 변환. 허용 밴드 ±0.05(팩 높이 비율, 폰에서 약 ±30px), 시작은 ±0.065, 좌우로 팩 밖 12%까지 허용. `getCoalescedEvents`로 프레임 사이 이동도 모두 반영. 95% 이상이면 완료 → 최근 0.1초 손가락 속도를 넘겨줌.
- `fx/cutFx.ts` — 커팅 헤드: 광점(halo+core 스프라이트, HDR) + 포인트 라이트 + 스파크 파티클(700개 링버퍼, CPU 시뮬레이션). 절단 진행량에 비례해 방출, 완료 순간 한 번 크게.

변경:

- `pack/pack.ts` — 팩을 윗조각(`top`)·본체(`body`)로 분리. 곡면 행을 전역 격자에 맞춰 겹치는 구간 삼각형이 두 조각에서 동일 → 찢김 곡선에서 픽셀 단위로 맞물림. 틈 사이로 보이는 은박 안감 띠 추가. `detachTop()` 후 윗조각은 씬으로 옮겨 속도·회전·중력으로 날아감(3초 후 숨김).
- `main.ts` — Bloom 후처리(Three.js `EffectComposer` + `UnrealBloomPass`, 임계값 1.0 → HDR 발광만 번짐, MSAA 4x 하프플로트 타깃). 자르는 동안 기울기 입력을 12%로 줄여 팩을 정면에 붙잡음. 절단 진행 시 롤·요 스프링에 반동을 줘 포장지가 끌리는 흔들림. 완료 시 윗조각이 손가락 속도·방향을 이어받아 날아가고(느리면 마지막 방향으로 기본 속도), 본체는 반동으로 살짝 내려감.
- **팩 탭 → 뒤집기 (사용자 제안, 명세 R10-1로 추가)**: 이동 10px·0.35초 이내면 탭. 스프링 회전 + 화면 쪽으로 떠오르는 궤적. 절취는 앞면일 때만.
- 안내 문구(하단): 앞면 "점선을 따라 옆으로 그어 개봉하세요 / 팩을 톡 누르면 뒷면을 볼 수 있어요", 뒷면 "뒷면이에요 / 팩을 톡 누르면 다시 앞면으로". 한 번 자르기 시작하면 사라짐.
- `gfx/backdrop.ts` — 후처리로 선형 값 출력이 되어 디더를 sRGB 기준으로 바꿈.
- 명세 5.1 후처리 항목을 실제 선택(Three.js 기본 애드온)으로 갱신.

## M3. 카드 뭉치 등장 + 탭 뒤집기 + 스와이프 넘기기 (2026-09-25, 사용자 검증 완료)

**결정 (사용자, 2026-09-25):** 카드는 뒷면으로 나온다 → 맨 위 카드를 탭하면 앞면으로 뒤집힘(뒷면 상태에서 옆으로 밀어도 뒤집힘) → 앞면 카드를 스와이프하면 날아가고 다음 카드 뒷면이 드러남. M0 시안의 "뒷면 테두리 레어 암시"와 맞추기 위함. 명세 R3·플로우 [4]에 반영.

추가된 파일:

- `card/textures.ts` — `design/lib/card.js`(M0 확정 시안) 앞면 5장 + 뒷면 1장을 788×1100으로 래스터화. 시안의 `data-maxw`(경품명·교환 조건 최대 폭)는 문서에 잠깐 붙여 실제 폰트로 폭을 잰 뒤 `textLength`로 줄임(`design/render.html`과 같은 규칙). 폰트는 팩과 같은 방식으로 SVG에 내장. 로딩 중에 `initTexture`로 GPU 업로드.
- `card/card.ts` — 카드 1장. 둥근 모서리 얇은 판(두께 0.0035, 앞/뒤/옆면 재질 분리). 계층 root(위치·기울기) → flipper(뒤집기). 위치·기울기·뒤집기 모두 스프링. 기울거나 뒤집히는 동안 아래 카드를 뚫지 않도록 회전량만큼 자동으로 들어 올림(`clearance()`). **재질은 M3 임시**(코팅 종이: 거칠기 0.46 + 클리어코트) — 등급별 금속·홀로는 M4.
- `card/deck.ts` — 카드 뭉치.
  - 처음부터 팩 안(절취선 아래, 앞·뒤 포장지 사이)에 뒷면으로 들어 있음 → 팩 앞면이 카드를 가리고 있다가 드러남(명세 R2 완료 기준).
  - 빠져나오기: 강성을 0.35→2.25Hz로 키우는 스프링 → 처음엔 버티다가 쑥 빠지고 끝에서 약 6% 튕김.
  - 화면 중앙으로: 씬으로 옮긴 뒤 0.3초 제자리(그동안 팩이 아래로 떨어져 빠짐 — 커지는 카드가 팩을 뚫지 않게), 이어서 중앙·확대(화면 높이 66% / 폭 80% 이내). 아래 카드는 살짝 흐트러져 쌓임.
  - 입력: 맨 위 카드를 레이캐스트로 판정. 탭(10px·0.35초 이내) → 뒤집기. 뒷면 카드는 손가락을 35%만 따라오고(고무줄), 일정 이상 밀면 그 방향으로 뒤집힘. 앞면 카드는 1:1로 따라오고, 놓을 때 카드 폭 1.6배/초 이상(되돌리는 방향 제외) 또는 폭 42% 이상 밀었으면 날아감(최소 6배/초, 날아가는 방향으로 회전), 아니면 놓은 속도를 이어받아 스프링 복귀.
  - 드래그 중 기울기: 속도 방향으로 기울고(y·x축), 끌고 간 거리만큼 화면 평면 회전(카드 윗부분을 잡았는지 아랫부분을 잡았는지에 따라 방향 반대).

변경:

- `main.ts` — 윗조각이 날아가고 0.35초 뒤 뭉치가 빠져나오기 시작 → 0.75초 뒤 팩 본체가 중력으로 떨어지며 뒤로 젖혀져 퇴장(그림자도 흐려짐). 카드용 그림자 추가. 개봉 후에는 드래그가 카드 넘기기용이라 기울이기는 마우스 호버 + 자동 흔들림만(넘기는 중엔 25%로 약하게). 안내 문구: "카드를 톡 눌러 뒤집어 보세요"(처음 뒤집을 때까지) → "옆으로 밀어서 다음 카드 보기"(처음 넘길 때까지) → 다 넘기면 "5장을 모두 확인했어요".
- `pack/pack.ts` — 속지(`lining`)를 필드로 노출. 카드가 빠져나올 때 숨김(z=0에 있어 카드와 겹침).

5장 모두 넘기면 안내 문구만 뜨고 멈춤 → 요약 화면은 M5.

**아직 없는 것 (M4에서):** 등급별 카드 재질(홀로·포일·패럴랙스), 다음 카드 뒷면 테두리 암시 발광(명세 R9), 뒤집을 때 등급별 등장 연출(반짝임·광선·플래시). → M4에서 구현.

## M4. 등급별 카드 재질 + 테두리 암시 발광 + 등장 연출 (2026-09-25)

### 카드 재질·테두리 암시 (커밋 `23b9284`)

- `card/holo.ts` — 등급별 재질 값(`RARITY_STYLE`: 금속도·거칠기·클리어코트)과 셰이더 삽입(`onBeforeCompile`).
  - 앞면 홀로: 보는 각도(프레넬)·위치·시간에 따라 흐르는 무지개 스트릭 → 기울이면 반응. 레어는 일러스트 창만, 슈퍼레어·최고등급은 전면. 세기는 등급별로 따로 조정(`HOLO_STRENGTH`, 글자·일러스트가 가려지지 않는 선).
  - 뒷면 테두리 암시 발광(명세 R9): 레어 은빛 / 슈퍼레어 금빛 / 최고등급 무지개, 테두리를 따라 천천히 일렁임. 뒷면 재질은 (텍스처, 등급) 쌍으로 캐시.
- **결정 (사용자, 2026-09-25):** 최고등급 반사 값은 슈퍼레어와 동일. 더 강하게 하면 밝은 파스텔 시안이 반사광에 하얗게 날아감.

### 뒤집을 때 등급별 등장 연출 (구현 완료, 사용자 검증 대기)

- `fx/revealFx.ts` — `RevealFx`. 명세 R9 표의 "등장 연출" 열 그대로:
  - 일반: 연출 없음(기본).
  - 레어: 은빛 반짝임 16개 — 카드 위 곳곳(절반은 테두리 근처)에서 0.9초에 걸쳐 시차를 두고 켜졌다 꺼지는 네 갈래 십자 광채.
  - 슈퍼레어: 금빛 반짝임 24개 (레어와 같은 방식, 색·개수만 다름).
  - 최고등급: 화면 플래시(0.05초 만에 45%까지 → 약 0.6초에 걸쳐 사그라듦) + 무지개 반짝임 26개 + 특수 연출(카드 둘레에서 퍼져 나가는 무지개 빛 고리 + 카드 외곽에서 사방으로 분출하는 빛 입자 90개).
  - **결정 (사용자, 2026-09-25):** 1차 값은 슈퍼레어·최고등급이 너무 강했음 → 구성은 유지하고 세기만 낮춤(광선 약 절반, 플래시 85→45%, 빛 고리·무지개 색 밝기 약 40%↓, 반짝임·입자 개수 축소). 이어서 뒤편 광선을 더 낮췄으나 광선 자체가 어울리지 않는다고 판단 → 방사형 광선을 줄무늬 없는 후광으로 바꿨지만 그것도 강하다고 해 **카드 뒤 광선·후광은 모두 제외**, 슈퍼레어도 레어처럼 카드 앞면 반짝임만(금빛) 쓰기로 함. 명세 R9 표도 갱신. 최고등급의 퍼져 나가는 빛 고리는 괜찮다고 해 유지.
- 시점: 뒤집히는 카드가 옆모습을 지나 앞면이 드러나기 시작하는 순간(`Deck.onReveal`, 뒤집기 진행도 50%). 최고등급은 플래시가 이 순간을 덮고 앞면이 드러남.
- 연출은 뒤집힌 카드(`card.root`)에 붙어 드래그하면 함께 움직임. 최고등급 빛 고리 판은 남은 뭉치보다 뒤에 있어 가운데는 카드가 가리고 가장자리 밖으로만 보임. 카드를 넘기면 0.3초에 걸쳐 거둠.
- 반짝임·입자는 방출 순간 값만 버퍼에 쓰고 궤적·명멸은 셰이더에서 계산(매 프레임 CPU 갱신 없음, 최대 320개). 밝은 부분은 HDR 값이라 Bloom으로 번짐.
- 화면 플래시는 캔버스 위 DOM 오버레이(`#flash`, 불투명도만 매 프레임 지정, 안내 문구보다 아래).
- 연출 셰이더도 로딩 중 `compileAsync`에 포함(첫 레어 카드 뒤집을 때 끊기지 않게).

**명세와 차이 (확인 필요):** 명세 R9 표의 최고등급 테두리 암시는 "무지개 테두리 발광 + 파티클"인데, 현재 뒷면 암시에는 파티클이 없고 발광만 있음.

## 다음 작업 (M5부터)

명세 6장 작업 단계 기준:

- **M5** — 요약 화면 + 선택·클로즈업 + 결과(쿠폰) 화면 → 여기까지가 "전체 플로우 완성 → 사장님 컨펌" 지점.

## 참고: 저장소에 이미 있던 파일

`AGENTS.md`, `README.md`, `.gitignore`, `.vscode/`는 이 세션 이전에 이미 저장소에 커밋되어 있었음(다른 세션/도구가 작성한 것으로 보임). **`AGENTS.md`는 커밋 시 claude/codex를 공동 작업자로 표기하지 말라고 명시** — 이 저장소에서 커밋할 때는 그 지침을 따름(harness의 기본 attribution 안내보다 우선).
